---
title: "Final Project"
author: "Group 43"
date: "2024-11-21"
output: github_document
---

```{r}
library(tidyverse)
library(ggplot2)
library(modelr)
library(readxl)
library(haven)
```

## Import Data

```{r, warning=FALSE, message=FALSE}
ratings = read_csv("data/coffee_ratings.csv") |> 
  janitor::clean_names() |> 
  select(total_cup_points, species, country_of_origin, 
         region, number_of_bags, bag_weight, grading_date, variety,
         processing_method, aroma, flavor, aftertaste, acidity, body, balance,
         uniformity, clean_cup, sweetness, cupper_points, moisture, color,
         altitude_low_meters, altitude_high_meters, altitude_mean_meters) |>
  mutate(grading_year = str_extract(grading_date, "\\d{4}"))


survey = read_csv("data/coffee_survey.csv") |> 
  janitor::clean_names()
```



```{r}
ratings$species = as.factor(ratings$species)
ratings$variety = as.factor(ratings$variety)
ratings$processing_method = as.factor(ratings$processing_method)
ratings$moisture = as.factor(ratings$moisture)
ratings$color = as.factor(ratings$color)
```








